# Quartz build output
public/
.quartz-cache/

# Node
node_modules/
npm-debug.log*
package-lock.json.bak

# Local sync target — content/ is generated by sync-vault.py and should not be checked in
# UNLESS you choose to commit the synced content for reproducibility (recommended for accreditation).
# Comment out the next line if you want to commit content/.
# content/

# Editor / OS
.DS_Store
.vscode/
.idea/
*.swp
*~

# Vault submodule (if used)
content-vault/

# Local-only role holders (never commit if you keep one)
scripts/role-holders.local.json
